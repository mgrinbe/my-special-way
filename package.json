{
	"devDependencies": {
		"lerna": "^2.11.0"
	},
	"scripts": {
		"build": "lerna run build",
		"bump": "lerna run bump",
		"docker": "lerna run docker",
		"deploy": "lerna run deploy",
		"push": "leran run push"
	}
}